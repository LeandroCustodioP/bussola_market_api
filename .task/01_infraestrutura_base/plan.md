# Plan: Infraestrutura Base

1. **Documentação de Decisão (ADR):** Utilizar a skill `@skills/registrar_adr.md` para registrar a decisão sobre o uso de arquivos `__init__.py` no arquivo `@.context/decisoes_tecnicas.md`.
2. **Criação de Estrutura:** Criar a árvore de diretórios (`src/`, `src/api/v1/`, `src/core/`, `src/db/`, `src/schemas/`, `test/`) incluindo os arquivos `__init__.py` em cada subpasta.
3. **Configurações de Raiz:** Criar arquivos `.gitignore`, `.env.example`, `README.md` e os arquivos de regras da IA (`.cursorrules`).
4. **Dependências:** Criar o arquivo `requirements.txt` com as bibliotecas base.
5. **Finalização:** Atualizar o `@.task/01_infraestrutura_base/tasks.md` marcando todos os itens como concluídos.